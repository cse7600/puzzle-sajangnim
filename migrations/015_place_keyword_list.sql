-- ============================================================
-- Migration 015: 대표 키워드 실제 목록 저장 (경쟁자 호버 비교 카드용)
-- 지금까지는 keyword_count(개수)만 저장했다. 호버 카드에 실제 키워드 칩을
-- 보여주려면 목록 자체가 필요하다.
-- ============================================================

alter table public.puzl_place_snapshots
  add column if not exists keyword_list text[]; -- 대표 키워드 실제 목록. null=못 읽음, []=미설정
